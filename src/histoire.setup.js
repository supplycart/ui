import Vue from 'vue'

// Global setup for Histoire stories
// Add any global components, plugins, or configurations here

// Example: Register global components if needed
// Vue.component('SomeGlobalComponent', SomeGlobalComponent)

// Example: Add global plugins
// Vue.use(SomePlugin)

// Setup global styles if needed
import './styles/address/address.css'